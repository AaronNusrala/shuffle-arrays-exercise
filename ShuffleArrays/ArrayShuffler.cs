using System;

namespace ShuffleArrays
{
	public class ArrayShuffler
	{
		/// <summary>
		/// Combine the two array parameters into a new array containing alternating elements from each.
		/// If one array is longer than the other, include all elements from the longer array after shuffling the matching elements.
		/// </summary>
		/// <param name="a"></param>
		/// <param name="b"></param>
		/// <returns></returns>
		public int[] Shuffle(int[] a, int[] b)
		{
			throw new NotImplementedException();
		}
	}
}
